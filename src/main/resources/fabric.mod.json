{
  "schemaVersion": 1,
  "id": "notifier",
  "version": "${version}",
  "name": "Notifier",
  "description": "",
  "authors": [],
  "contact": {},
  "license": "GPL-3.0",
  "icon": "assets/notifier/ping.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "pw.znopp.notifier.client.NotifierClient"
    ],
    "main": [
      "pw.znopp.notifier.Notifier"
    ]
  },
  "mixins": [
    "notifier.mixins.json",
    {
      "config": "notifier.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
